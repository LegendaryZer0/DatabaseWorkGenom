/*
package org.example;

import org.flywaydb.core.Flyway;

public class FlywayFirstMigrate {
    public static void main(String[] args) {
        // Create the Flyway instance and point it to the database
        Flyway flyway = Flyway.configure().dataSource("jdbc:postgresql://localhost:5432/handsome", "handsome", "DxFcGHJ16").load();


        // Start the migration
        flyway.migrate();
    }
}
*/
